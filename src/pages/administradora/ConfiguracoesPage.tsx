import React from 'react';
import { Settings } from '@/pages/Settings';

// Página de configurações isolada para administradora
// Usa o mesmo componente Settings mas está em rota isolada
export default function ConfiguracoesPage() {
  return <Settings />;
}
